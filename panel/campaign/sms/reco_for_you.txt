D-TELCO: picked for you, {%= $from("product").where("product_id", "=", $Contact.reco_product_id_1).value("title") %} at ${%= $from("product").where("product_id", "=", $Contact.reco_product_id_1).value("discounted_price") %}. {%= $Current.link || 'https://d-telco.github.io/telco/' %} STOP to opt out.
